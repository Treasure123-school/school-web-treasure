# ===========================================
# DEVELOPMENT ENVIRONMENT (Replit)
# ===========================================
# ✅ NO CONFIGURATION NEEDED FOR REPLIT DEVELOPMENT
# The system automatically detects Replit environment and configures:
# - VITE_API_URL: Auto-set to same-origin (empty string)
# - Google OAuth callback: Uses REPLIT_DEV_DOMAIN automatically
# - CORS: Auto-configured for Replit domains
#
# Required environment variables (set in Replit Secrets):
# - DATABASE_URL (from Supabase/Neon)
# - SESSION_SECRET
# - JWT_SECRET
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET

# ===========================================
# LOCAL DEVELOPMENT (Localhost)
# ===========================================
# For local development only:
NODE_ENV=development
DATABASE_URL=postgresql://user:password@localhost:5432/database
SESSION_SECRET=your-dev-session-secret
JWT_SECRET=your-dev-jwt-secret
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# VITE_API_URL is auto-set to empty string (same-origin)

# ===========================================
# PRODUCTION ENVIRONMENT (Render + Vercel)
# ===========================================
# Backend Environment Variables (Render):
DATABASE_URL=postgresql://user:password@host:port/database
SESSION_SECRET=your-production-session-secret
JWT_SECRET=your-production-jwt-secret
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
NODE_ENV=production
BACKEND_URL=https://your-backend.onrender.com
FRONTEND_URL=https://your-app.vercel.app

# Frontend Environment Variables (Vercel):
VITE_API_URL=https://your-backend.onrender.com

# ===========================================
# ENVIRONMENT PRIORITY & AUTO-CONFIGURATION
# ===========================================
# 1. DEVELOPMENT (Replit): Uses REPLIT_DEV_DOMAIN for OAuth callbacks
# 2. PRODUCTION: Uses BACKEND_URL for OAuth callbacks
# 3. LOCAL: Uses localhost for OAuth callbacks
#
# Google OAuth Callback URL Priority:
# - Replit: https://{REPLIT_DEV_DOMAIN}/api/auth/google/callback
# - Production: https://{BACKEND_URL}/api/auth/google/callback
# - Local: http://localhost:5000/api/auth/google/callback
#
# Frontend API URL Priority (vite.config.ts):
# - Production (Vercel): Uses VITE_API_URL env var
# - Development (Replit/Local): Uses empty string (same-origin)
